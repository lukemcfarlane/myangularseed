/**
 * Jasmine test specs.
 *
 * @author  AUTHOR
 * @date    DATE
 */
describe('the service', function() {
	beforeEach(module('MyApp'));

	beforeEach(inject(function() {}));

	it('should do this');

	it('should do that');
});